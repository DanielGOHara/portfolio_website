
import React from "react";

export default function Skills() {
    return (
        <section className="buttonContainer">
            <div className="btn pageButton" role="button" onClick={() => alert("Skills")}>
                <span className="buttonName" class="text">Skills</span>
            </div>
        </section>
    );
}